name: headtracker
description: ARKit head tracking for flight and racing sims
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  arkit_plugin: ^1.0.4
  shared_preferences: ^2.2.0 # Add this line here

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
